server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Proxy all backend API calls to the FastAPI server running on port 8000.
    # The /api prefix is stripped before forwarding (note the trailing slash on proxy_pass).
    location /api/ {
        proxy_pass         http://localhost:8000/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Serve the React SPA — fall back to index.html for client-side routes.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
